cat > database/.env.example <<'EOF'
# Example DB env (do NOT commit real secrets to repo)
DB_USER=postgres
DB_PASSWORD=mypassword123
DB_NAME=bank_reviews

IMPORTANT: Use 127.0.0.1 to avoid Docker networking issues (like resolving to ::1)

DB_HOST=127.0.0.1
DB_PORT=5432
EOF
